<preference-headers xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- These settings headers are only used on tablets. -->

    <header
        android:fragment="${packageName}.${activityClass}$ActsPreferenceFragment"
        android:title="@string/pref_acts_header" />
    <header
        android:fragment="${packageName}.${activityClass}$DicePreferenceFragment"
        android:title="@string/pref_support_dice_header" />
      <header
        android:fragment="${packageName}.${activityClass}$ThemePreferenceFragment"
        android:title="@string/pref_theme_header" />

</preference-headers>